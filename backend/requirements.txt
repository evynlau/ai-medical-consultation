# AI 智能问诊系统 - Python 依赖清单
# 维护原则:只列运行代码真正 import 的库,版本取 PyPI 现行稳定版下限

# ===== Web 框架 =====
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ===== 数据库 =====
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0          # PostgreSQL(可选,部署时启用)

# ===== 缓存(可选) =====
redis>=5.0.1

# ===== LLM(OpenAI 兼容协议) =====
openai>=1.10.0

# ===== LangChain 生态 =====
# 用途:MedicalAgent 已重构为 LangGraph StateGraph + BaseChatModel
# Agent 内部使用 ChatOpenAI / FakeListChatModel;OCR/影像继续走原 llm_service
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
langchain-community>=0.3.0

# ===== 向量检索 =====
faiss-cpu>=1.8.0
numpy>=1.26.0,<3.0.0    # FAISS 1.x 不支持 numpy 2.x

# ===== 医学影像推理(在线服务) =====
# 用途:xrv DenseNet121 + Grad-CAM + PSPNet 肺部分割
torch>=2.0.0,<3.0.0
torchvision>=0.15.0
torchxrayvision>=0.0.9
opencv-python>=4.8.0
Pillow>=10.0.0

# ===== Embedding(本地模型,sentence-transformers) =====
# 启动后才会下载 ~120MB;EMBEDDING_PROVIDER=mock 时不需要
sentence-transformers>=2.3.1

# ===== 训练脚本(可选,生产部署不需要) =====
# 用途:backend/scripts/train_pneumonia.py + train hospital_model.py
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.66.0

# ===== 认证(JWT + 密码哈希) =====
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# ===== 配置与数据校验 =====
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0

# ===== 文档解析(可选,知识库构建) =====
# 用途:把 PDF 教科书/指南导入知识库
pymupdf>=1.23.0

# ===== OCR - 处方/检查报告图片识别(可选) =====
# 依赖系统包:
#   Linux:  sudo apt-get install -y tesseract-ocr tesseract-ocr-chi-sim
#   macOS:  brew install tesseract tesseract-lang
pytesseract>=0.3.10

# ===== 工具库 =====
httpx>=0.26.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
nest_asyncio>=1.5.0   # 让 RAG 服务在已有事件循环中安全地跑异步 embedding
